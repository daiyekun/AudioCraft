<Window x:Class="AudioCraft.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AudioCraft"
        mc:Ignorable="d"
        Title="小胖子专属工具 - 音频格式转换器" Height="850" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5"
        AllowDrop="True"
        Drop="Window_Drop"
        DragEnter="Window_DragEnter"
        DragLeave="Window_DragLeave"
        Loaded="Window_Loaded" Icon="/app.png" SizeChanged="Window_SizeChanged">

    <Window.Resources>
        <!-- 微信绿色主题 -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#07c160"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#06ad56"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#f5f5f5"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#ffffff"/>
        <SolidColorBrush x:Key="TextBrush" Color="#333333"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#999999"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#e5e5e5"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#07c160"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#fa5151"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#ffc300"/>

        <!-- 主按钮样式 -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#cccccc"/>
                                <Setter Property="Foreground" Value="#999999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 次要按钮样式 -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#f0f0f0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 下拉框样式 -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- GroupBox样式 -->
        <Style x:Key="CardGroupBox" TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" 
                                    Background="{StaticResource SurfaceBrush}" 
                                    CornerRadius="8,8,0,0" 
                                    Padding="16,12"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,1,1,0">
                                <ContentPresenter ContentSource="Header"/>
                            </Border>
                            <Border Grid.Row="1" 
                                    Background="{StaticResource SurfaceBrush}" 
                                    CornerRadius="0,0,8,8" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,0,1,1"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="24,16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="小胖子专属工具" 
                               FontSize="22" 
                               FontWeight="SemiBold" 
                               Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="音频格式转换工具" 
                               FontSize="12" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="格式：" 
                               Foreground="{StaticResource TextSecondaryBrush}" 
                               VerticalAlignment="Center" 
                               Margin="0,0,8,0"/>
                    <ComboBox x:Name="cmbSourceFormat" 
                              Style="{StaticResource ComboBoxStyle}"
                              Width="90" 
                              Margin="0,0,8,0"
                              SelectedIndex="0">
                        <ComboBoxItem Content="AWB"/>
                        <ComboBoxItem Content="WAV"/>
                        <ComboBoxItem Content="FLAC"/>
                        <ComboBoxItem Content="OGG"/>
                    </ComboBox>
                    <TextBlock Text="→" 
                               Foreground="{StaticResource TextSecondaryBrush}" 
                               FontSize="16" 
                               VerticalAlignment="Center" 
                               Margin="4,0"/>
                    <ComboBox x:Name="cmbTargetFormat" 
                              Style="{StaticResource ComboBoxStyle}"
                              Width="90" 
                              Margin="8,0,0,0"
                              SelectedIndex="1">
                        <ComboBoxItem Content="WAV"/>
                        <ComboBoxItem Content="MP3"/>
                        <ComboBoxItem Content="FLAC"/>
                        <ComboBoxItem Content="AAC"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="260"/>
            </Grid.ColumnDefinitions>

            <!-- Drop Zone / File List -->
            <Border Grid.Column="0" 
                    x:Name="dropZone"
                    Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="8"
                    BorderThickness="1"
                    BorderBrush="{StaticResource BorderBrush}"
                    Margin="0,0,16,0">
                <Grid>
                    <!-- Empty State -->
                    <StackPanel x:Name="emptyState" 
                                VerticalAlignment="Center" 
                                HorizontalAlignment="Center"
                                Visibility="Visible">
                        <TextBlock Text="📂" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        <TextBlock Text="拖拽音频文件到此处" 
                                   FontSize="16" 
                                   Foreground="{StaticResource TextBrush}" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="支持 AWB、WAV、FLAC、OGG 格式" 
                                   FontSize="13" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   HorizontalAlignment="Center" 
                                   Margin="0,6,0,0"/>
                    </StackPanel>

                    <!-- File List -->
                    <ListView x:Name="lstFiles" 
                              Grid.Row="1" 
                              Background="Transparent" 
                              BorderThickness="0"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              Visibility="Collapsed"
                              BorderBrush="{StaticResource BorderBrush}">
                        <ListView.View>
                            <GridView>
                                <GridView.ColumnHeaderContainerStyle>
                                    <Style TargetType="GridViewColumnHeader">
                                        <Setter Property="Background" Value="#fafafa"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                        <Setter Property="Padding" Value="16,10"/>
                                        <Setter Property="FontSize" Value="13"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="GridViewColumnHeader">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GridView.ColumnHeaderContainerStyle>
                                <GridViewColumn Header="文件名" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FileName}" 
                                                       Foreground="{StaticResource TextBrush}" 
                                                       TextTrimming="CharacterEllipsis" 
                                                       FontSize="13"
                                                       Margin="0,6"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="大小" Width="60">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FileSize}" 
                                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                                       FontSize="13"
                                                       Margin="0,6"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="状态" Width="70">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Status}" 
                                                       Foreground="{Binding StatusBrush}" 
                                                       FontSize="13"
                                                       Margin="0,6"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="进度" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="0,6,8,6">
                                                <ProgressBar Value="{Binding Progress}" 
                                                             Maximum="100" 
                                                             Height="6"
                                                             Background="#e5e5e5"
                                                             Foreground="{StaticResource PrimaryBrush}"/>
                                                <TextBlock Text="{Binding Progress, StringFormat={}{0:0}%}" 
                                                           HorizontalAlignment="Right"
                                                           FontSize="11"
                                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="操作" Width="60">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="×"
                                                    FontSize="16"
                                                    Foreground="{StaticResource ErrorBrush}"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Click="BtnRemoveFile_Click"
                                                    Tag="{Binding}"
                                                    Padding="4,0">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border x:Name="border" Background="Transparent" CornerRadius="4" Padding="8,4">
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter TargetName="border" Property="Background" Value="#ffe5e5"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- Drag Overlay -->
                    <Border x:Name="dragOverlay" 
                            Background="#2007c160" 
                            CornerRadius="8" 
                            Visibility="Collapsed"
                            BorderThickness="2"
                            BorderBrush="{StaticResource PrimaryBrush}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="松开鼠标添加文件" 
                                       FontSize="18" 
                                       Foreground="{StaticResource PrimaryBrush}" 
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Right Panel -->
            <ScrollViewer Grid.Column="1" 
                          Width="260"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <!-- Add Files Button -->
                    <Button Style="{StaticResource PrimaryButton}" 
                            Click="BtnAddFiles_Click"
                            Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="+" FontSize="16" FontWeight="Bold" Margin="0,0,6,0"/>
                            <TextBlock Text="添加文件"/>
                        </StackPanel>
                    </Button>

                    <!-- Source Folder Group -->
                    <GroupBox Header="原始文件目录" 
                              Style="{StaticResource CardGroupBox}">
                        <StackPanel>
                            <TextBlock x:Name="txtSourcePath" 
                                       Text="未选择目录" 
                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                       TextTrimming="CharacterEllipsis"
                                       FontSize="13"
                                       Margin="0,0,0,12"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource SecondaryButton}" 
                                        Click="BtnChangeSource_Click"
                                        Margin="0,0,8,0">
                                    <TextBlock Text="选择目录" FontSize="13"/>
                                </Button>
                                <Button Style="{StaticResource SecondaryButton}" 
                                        Click="BtnLoadSource_Click">
                                    <TextBlock Text="加载文件" FontSize="13"/>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <!-- Output Folder Group -->
                    <GroupBox Header="目标目录" 
                              Style="{StaticResource CardGroupBox}">
                        <StackPanel>
                            <TextBlock x:Name="txtOutputPath" 
                                       Text="未选择目录" 
                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                       TextTrimming="CharacterEllipsis"
                                       FontSize="13"
                                       Margin="0,0,0,12"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource SecondaryButton}" 
                                        Click="BtnChangeOutput_Click"
                                        Margin="0,0,8,0">
                                    <TextBlock Text="选择目录" FontSize="13"/>
                                </Button>
                                <Button Style="{StaticResource SecondaryButton}" 
                                        Click="BtnOpenOutput_Click">
                                    <TextBlock Text="打开目录" FontSize="13"/>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <!-- Convert and Clear Buttons -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="btnConvert"
                                Grid.Column="0"
                                Style="{StaticResource PrimaryButton}" 
                                Click="BtnConvert_Click"
                                IsEnabled="False"
                                Height="44"
                                Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="▶" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="开始转换" FontSize="15"/>
                            </StackPanel>
                        </Button>
                        <Button Grid.Column="1"
                                Click="BtnClear_Click"
                                Height="44"
                                Padding="16,0"
                                Cursor="Hand"
                                VerticalContentAlignment="Center">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border" 
                                                        Background="#ff9800" 
                                                        CornerRadius="6"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#e68a00"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="border" Property="Background" Value="#cccccc"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="清空" 
                                       Foreground="#ffffff" 
                                       FontSize="14" 
                                       FontWeight="SemiBold"/>
                        </Button>
                    </Grid>

                    <!-- Log Panel -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="8" 
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderBrush}"
                            Padding="0"
                            Height="200">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Padding="12,10" Background="#fafafa" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" CornerRadius="8,8,0,0">
                                <TextBlock Text="转换日志" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                            </Border>
                            <ScrollViewer x:Name="logScroller" Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="12,8">
                                <TextBox x:Name="txtLog" 
                                         Background="Transparent" 
                                         Foreground="{StaticResource TextSecondaryBrush}"
                                         BorderThickness="0"
                                         IsReadOnly="True"
                                         TextWrapping="Wrap"
                                         FontFamily="Microsoft YaHei"
                                         FontSize="12"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" Padding="20,10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="txtStatus" 
                           Text="就绪" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12"/>
                <TextBlock Grid.Column="1" 
                           x:Name="txtFileCount" 
                           Text="0 个文件" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12"/>
            </Grid>
        </Border>
    </Grid>
</Window>
